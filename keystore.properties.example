# Copy this file to keystore.properties and fill in real values, then run: gradlew.bat bundleRelease
# keystore.properties is git-ignored so your secrets never get committed.
storeFile=upload-keystore.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
